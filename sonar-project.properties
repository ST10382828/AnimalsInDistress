# SonarQube Project Configuration
# This file will be used when running local SonarQube analysis
# For SonarCloud, we'll use the web interface or token-based authentication

sonar.projectKey=ST10382828_AnimalsInDistress
sonar.organization=st10382828
sonar.projectName=Animals In Distress
sonar.projectVersion=1.0

# Source code location
sonar.sources=app/src/main/java
sonar.tests=app/src/test,app/src/androidTest

# Language
sonar.language=kotlin

# Exclusions (files/folders to ignore)
sonar.exclusions=**/build/**,**/generated/**,**/*.xml,**/*.json,**/*.gradle*

# Coverage reports (if available)
# sonar.coverage.jacoco.xmlReportPaths=app/build/reports/jacoco/test/jacocoTestReport.xml

# Encoding
sonar.sourceEncoding=UTF-8

# Kotlin specific
sonar.kotlin.detekt.reportPaths=build/reports/detekt/detekt.xml

